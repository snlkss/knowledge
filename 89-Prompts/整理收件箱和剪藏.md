# 整理收件箱和剪藏

请整理这个 Obsidian 知识库里的 `00-Inbox`、`00-Inbox/Downloaded` 和 `Clippings`。

要求：

1. 不改原文核心意思。
2. 保留标题、作者、平台、来源链接、抓取时间。
3. 按主题移动到 `04-Research`，按项目移动到 `06-Projects`，按成稿移动到 `05-Content`。
4. 将 `clippings` 这类临时标签替换成有意义的主题标签。
5. 整理完成后说明移动了哪些文件、还有哪些资料需要人工判断。
